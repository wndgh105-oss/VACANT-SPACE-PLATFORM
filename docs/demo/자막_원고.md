# 빈자리 데모 영상 — 자막 원고 및 스토리보드

> 2026-09-14 개정: 실사 승인·인앱 알림 기능 추가에 맞춰 재녹화. 이전 버전은 59초, 13장면이었고
> 지금은 운영자 실사 승인 → 건물주/창업자 알림 확인 장면이 추가돼 22장면으로 늘었습니다.
>
> 2026-09-14 재재녹화: 정식 창업 비용에 간판·집기 비용이 반영되면서(`startupBaseline.ts`)
> 절감액 수치가 8,100만 원 → **8,606만 원**으로 바뀌어 자막을 다시 맞췄습니다.
>
> 2026-09-14 4차 녹화: 랜딩 히어로 사진을 흰 외벽 카페 매장 외관 사진으로 교체하면서 재녹화.
> 이 과정에서 `resetDemoState()`가 이전 녹화의 Tenancy(계약 확정 기록)를 지우지 않아 X17
> 중복 계약 가드에 막혀 녹화가 실패하는 버그를 발견해 `scripts/record-demo.ts`에서 함께 고쳤다.
> 서버가 웜업된 상태라 전체 길이가 69.9초로 줄어 자막 타이밍도 다시 비례 배분했다.

- **길이**: 69.9초, 22장면 (1280×800, 25fps)
- **원본**: `docs/demo/raw/*.webm` (Playwright 실제 브라우저 자동 조작 녹화)
- **변환본**: `docs/demo/demo.mp4` (H.264 / 3.81MB / 재생 확인 완료)
- **장면 스크린샷**: `docs/demo/scenes/01~22*.png`
- **녹화 스크립트**: `scripts/record-demo.ts` / **변환 스크립트**: `scripts/build-demo.mjs`

> 이 영상은 **실제로 실행 중인 MVP를 자동 조작해 녹화**한 것입니다. 정지 화면을 이어 붙인 것이 아닙니다.
> 다만 마지막 클로징 카드만 영상 아웃트로용 오버레이로 삽입했습니다(`outro()` 함수).
> 영상 속 결제·계약은 실제로 이루어지지 않으며, 화면 상단에 데모 고지가 상시 노출됩니다.

---

## 장면 구성

| # | 시간(약) | 화면 | 실제 동작 |
|---|---|---|---|
| 1 | 0:00–0:05 | 랜딩 히어로 | "1억을 걸기 전에, 두 달만 장사해 보세요" |
| 2 | 0:05–0:11 | 비용 비교 블록 | 정식 창업 1억 750만 원 vs 빈자리 628만 원 스크롤 |
| 3 | 0:11–0:16 | 퀵 검색 | 성수동 / 2개월 / 150만 원 이하 / 카페 선택 후 검색 |
| 4 | 0:16–0:21 | 검색 결과 | 조건에 맞는 공간 카드 등장 |
| 5 | 0:21–0:25 | 지도 보기 | 목록 ⇄ 지도 전환, 가격 핀 표시 |
| 6 | 0:25–0:30 | 공간 상세 | 연무장길 공간 조건·상권 요약·추천 업종 |
| 7 | 0:30–0:37 | 견적 구성 | 2개월 선택 → 카페 스타터 패키지 선택 → 품목 확인 |
| 8 | 0:37–0:42 | 견적 합계 | 총비용·필요 현금·정식 창업 대비 절감액 표시 |
| 9 | 0:42–0:48 | 상담 요청 | 폼 입력 + 계약 전 확인 체크 → 요청 접수 완료 |
| 10 | 0:48–0:51 | 창업자 대시보드 | 운영 중 계약, D-day, 다음 선택지 |
| 11 | 0:51–0:56 | 이전 티저 | "우리가 어디로 옮겼게요?" 힌트·흐린 지도·공유 |
| 12 | ~ | 운영자 대시보드 | 현장 실사 대기열, "실사 승인" 클릭 |
| 13 | ~ | 건물주 대시보드 | 알림 벨 — "실사를 통과했어요" 알림 확인 |
| 14 | ~ | 건물주 대시보드 | 공실 → 상담 → 계약 확정 전환 |
| 15 | ~ | 창업자 대시보드 | 알림 벨 — "계약 확정" 상태 변경 알림 확인 |
| 16 | ~ | 클로징 | "공실은 기회가 되고, 창업은 가벼워집니다" |

### 신규 추가 장면 (2026-09-14)

운영 대시보드의 **현장 실사 승인 플로우**와 **인앱 알림(🔔)** 기능은 9/4 첫 녹화 이후 추가된 기능이라
이번 재녹화에서 새로 포함했습니다.

1. 운영자(admin@demo.kr)로 로그인 → `/admin/overview`의 "현장 실사 대기열"에서 "실사 승인" 클릭
2. 건물주(landlord@demo.kr)로 로그인 → 헤더 알림 벨을 열어 "공실이 실사를 통과했어요" + "새 상담 요청이 도착했어요" 확인
3. 기존과 동일하게 상담 시작 → 계약 확정 진행
4. 창업자(tenant@demo.kr)로 로그인 → 알림 벨을 열어 "신청 상태가 바뀌었어요"(상담 중 → 계약 확정) 확인

---

## 자막 원고 (내레이션 겸용)

> 아래 문구는 화면에 얹는 자막이자 나레이션 대본입니다.
> `docs/demo/demo.srt` 에 같은 내용이 SRT 형식으로 들어 있습니다.
> 큐 타이밍은 `scripts/record-demo.ts`의 대기 시간을 실측 78.1초에 맞춰 비례 배분한 추정치입니다.

| # | 시간(약) | 자막 |
|---|---|---|
| 1 | 0:00 | 서울에서 카페 하나를 열려면 약 1억 원이 듭니다. |
| 2 | 0:04 | 그런데 그 자리에서 커피가 팔릴지는 아무도 확인해 보지 못했습니다. |
| 3 | 0:07 | 계약 단위가 2년이라, 확인할 방법 자체가 없었으니까요. |
| 4 | 0:09 | 빈자리는 그 단위를 쪼갭니다. 지역·기간·예산·업종으로 공실을 찾습니다. |
| 5 | 0:13 | 목록과 지도를 오가며 위치를 비교합니다. |
| 6 | 0:15 | 상권 요약과 추천 업종까지 확인합니다. |
| 7 | 0:21 | 2개월을 고르고 카페 장비를 패키지로 더합니다. 구매가 아니라 렌탈이라, 실패해도 반납하면 끝입니다. |
| 8 | 0:25 | 총 얼마인지 한 화면에 나옵니다. 정식 창업보다 **8,606만 원** 적게 듭니다. |
| 9 | 0:30 | 상담을 요청합니다. 결제도, 법적 계약도 아직 아닙니다. |
| 10 | 0:35 | 운영 중에는 남은 기간과 다음 선택지가 보입니다. |
| 11 | 0:37 | 더 좋은 자리로 옮길 땐, 단골이 직접 찾아오게 만듭니다. |
| 12 | 0:43 | 운영자가 현장 실사를 확인하고 승인합니다. |
| 13 | 0:47 | 건물주에게 알림이 가고, 공실은 계약으로 바뀝니다. |
| 14 | 0:53 | 상담에서 계약 확정까지, 전체 과정이 화면 안에서 이어집니다. |
| 15 | 0:58 | 창업자에게도 계약 확정 알림이 실시간으로 도착합니다. |
| 16 | 1:04 | 공실은 기회가 되고, 창업은 가벼워집니다. |

### 영상에서 쓰지 않은 표현 (의도적 배제)

오해를 만들 수 있어 아래 표현은 사용하지 않았습니다.

- "지금 바로 계약하세요", "결제 완료" 등 실제 거래가 일어난 듯한 문구
- "합법적으로 보장됩니다", "법적 문제 없습니다" 등 법률 판단 단정
- "실제 매물", "현재 임대 중인 공간" 등 실재 매물처럼 보이는 표현
- 실존 브랜드·플랫폼의 명칭이나 화면

---

## 재생성 방법

```bash
npm run dev          # 별도 터미널에서 개발 서버 실행
npm run seed         # 데모 데이터 준비 (한 번만)
npm run demo         # 녹화 + mp4 변환 (= demo:record && demo:build)
```

### mp4 변환

`scripts/build-demo.mjs` 는 ffmpeg를 아래 순서로 찾습니다.

1. `FFMPEG_PATH` 환경변수
2. 시스템 PATH의 `ffmpeg`
3. Playwright 번들 ffmpeg

⚠️ **Playwright 번들 ffmpeg는 webm(VP8) 전용 축소 빌드라 mp4 muxer가 없습니다.**
따라서 mp4 산출물을 만들려면 **일반 ffmpeg가 설치되어 있어야 합니다.**

```bash
# Windows
winget install Gyan.FFmpeg
# macOS
brew install ffmpeg
```

설치 후 `npm run demo:build` 를 다시 실행하면 `docs/demo/demo.mp4` 가 생성됩니다.
ffmpeg가 없는 환경에서는 `docs/demo/raw/*.webm` 파일을 그대로 재생하면 됩니다
(크롬·엣지·파이어폭스에서 바로 재생됩니다).
